import { SetPositionDirective } from './set-position.directive';

describe('SetPositionDirective', () => {
  it('should create an instance', () => {
    const directive = new SetPositionDirective();
    expect(directive).toBeTruthy();
  });
});
